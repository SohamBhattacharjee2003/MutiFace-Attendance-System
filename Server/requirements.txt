# Presence AI — backend dependencies
#
# Everything the system actually imports, and nothing it doesn't.

# ── Face recognition: detection (RetinaFace/SCRFD) + ArcFace 512-d embeddings ──
insightface==1.0.1
onnxruntime==1.27.0          # CPU inference. Swap for onnxruntime-gpu if a GPU is present.

# ── Numerics + model artefacts (centroids, label encoder). No classifier: see trainer.py 
scikit-learn==1.8.0
numpy==2.4.3
joblib==1.5.3
opencv-python==4.13.0.92
pillow==12.1.1

# ── Attendance reports (scripts + /api/classes/<id>/export) ───────────────────
openpyxl==3.1.5              # .xlsx export — colleges run on spreadsheets

# ── Web API ───────────────────────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.5
itsdangerous==2.2.0          # signs the auth tokens
Werkzeug==3.1.8              # password hashing

# ── Evaluation / benchmarking scripts (scripts/) ──────────────────────────────
matplotlib==3.10.8           # ablation / degradation charts, PPT figures
tqdm==4.67.3
psutil==7.2.2                # scripts/benchmark.py
requests==2.33.0             # scripts/test_multiface.py
