# Presence AI — backend dependencies
#
# The previous version of this file did not list insightface or onnxruntime — the two
# packages the entire recognition pipeline runs on — so a fresh clone could not start it.
# It did list torch, torchvision and mtcnn, which the ArcFace pipeline never imports
# (~3 GB of unused downloads). Both problems are fixed here.

# ── Face recognition: detection (RetinaFace/SCRFD) + ArcFace 512-d embeddings ──
insightface==1.0.1
onnxruntime==1.27.0          # CPU inference. Swap for onnxruntime-gpu if a GPU is present.

# ── Classifier, embeddings, numerics ──────────────────────────────────────────
scikit-learn==1.8.0
numpy==2.4.3
joblib==1.5.3
opencv-python==4.13.0.92
pillow==12.1.1

# ── Web API ───────────────────────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.5
itsdangerous==2.2.0          # signs the auth tokens
Werkzeug==3.1.8              # password hashing

# ── Evaluation / benchmarking scripts (scripts/) ──────────────────────────────
matplotlib==3.10.8           # confusion matrix, ROC, degradation curves
tqdm==4.67.3
psutil==7.2.2                # scripts/benchmark.py
requests==2.33.0             # scripts/test_multiface.py

# ── Legacy dlib pipeline (OPTIONAL — not used by the web app) ─────────────────
# Only scripts/attendance.py and the unused fallback in api.py touch these. The deployed
# path is ArcFace and never imports them. dlib compiles from source and is slow to install,
# so install it only if you want the 128-d baseline for the paper's comparison:
#     pip install dlib==20.0.0 face-recognition==1.3.0
